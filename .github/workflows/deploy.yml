# このワークフローはNetlify移行により無効化されました
# Netlifyでは直接Gitリポジトリから自動デプロイが行われます
# 
# Netlify設定:
# - Build command: npm run build:netlify
# - Publish directory: dist
# - Auto-deploy: main ブランチへのpush時
#
# 削除する場合は、このファイル全体を削除してください

name: Netlify Deploy (Disabled)

# on:
#   push:
#     branches: [ main ]

# このワークフローは実行されません
on: []

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
    - name: Netlify Migration Info
      run: |
        echo "このプロジェクトはNetlifyに移行されました"
        echo "GitHub Actionsによる自動デプロイは無効化されています"
        echo "デプロイはNetlifyが自動で行います"
          
